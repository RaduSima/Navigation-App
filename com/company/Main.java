package com.company;

/**
 * The class that loads first.
 */
public class Main {

    /**
     * The method that runs first.
     *
     * @param args Array of strings representing the command line argument
     *             strings.
     */
    public static void main(String[] args) {
        Application app = new Application();
        app.run();
    }
}